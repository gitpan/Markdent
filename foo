use strict;
use warnings;

use File::Slurp qw( edit_file );

edit_file(
    sub {
        if (s/use namespace::autoclean;\n//) {
            s/(use warnings;\n)/${1}use namespace::autoclean;\n/;
        }
    },
    $_
) for @ARGV;
